[package]
name = "uasset-parser-wasm"
version = "0.1.0-rc.4"
edition = "2024"
rust-version = "1.85"
description = "WebAssembly binding for UE Shed's read-only UAsset parser"
license = "MIT"
repository = "https://github.com/ue-shed/ue-shed"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
uasset-parser = { path = "../uasset-parser" }
wasm-bindgen = "0.2"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"
