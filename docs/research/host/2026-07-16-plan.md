# 2026-07-16: sequencing toward the adoption demo

Status: agreed direction. Companion to [`design.md`](design.md); this document decides what to do
next, given that a demo is wanted soon and the host/adoption flow must be part of it.

## The call

No new slices, and no broad polish across all existing slices. Go **depth on one slice plus the
adoption seam**: for this demo the adoption flow is the headline feature, and Data Authoring is
the one slice that has to carry it.

- **New slices: no.** Every domain added before the seam exists increases the composition
  knowledge an adopter must reverse-engineer, and dilutes demo quality across more surface. New
  slices multiply in value _after_ the manifest/adoption machinery exists, because each one lands
  as an adoption target from day one.
- **Finish-everything-first: no.** One excellent vertical beats four decent ones, and the showcase
  home already sells four stories. Data Authoring is the flagship, the furthest along, and the
  adoption proving case named in `design.md`. Game Text and Texture Audit are demo-adequate as
  supporting cast; Map Review and Camera Lab get no investment before this demo.
- **Plan 007 waits, again for the migrate-twice reason.** The repository already paused 007 once
  so conflicts/rich types would not be built on pre-Effect services. The same logic now applies to
  the host seam: 007 is XL and would grow workflow code inside the current Workbench-service
  shape, which plan 016 would then relocate. 007 lands on the new shape, after the demo. The
  current draft → apply → save loop already demos honestly without it.

## Order of work

1. **Finish the Effect track (plans 014, 015).** Non-negotiable foundation; 014 is mid-flight in
   the working tree.
2. **Finish plans 004 and 005 (both IN PROGRESS).** The session service and the Peculiar Sheets
   draft editor are not optional polish — they are the flagship slice's UX. If they are rough, the
   demo is rough regardless of the adoption story.
3. **Plan 016, demo-scoped.** The demo-critical path is narrower than the full `design.md` scope:
    - a direct in-process `AuthoringClientLive`;
    - a slim `ShedHostLive` covering the authoring path;
    - the minimal non-Electron example host;
    - the Data Authoring slice manifest plus per-slice agent doc.

    Trailing after the demo (same seam, more of it): the generalized transport bridge, the CLI
    re-adapter rewrite, manifests for the remaining slices, the camera-lab slice move, and the
    complete Workbench refactor.

4. **Run the fresh-agent adoption eval before the demo, not during it.** Live agent runs are
   nondeterministic and slow — the wrong thing to bet a room on. Pre-run it and demo the artifact:
   the blank project, the manifest, the slice running inside it. For live proof of ownership,
   edit the copied StyleX tokens in the adopted project and watch it restyle — divergence made
   visible in thirty seconds, no agent latency involved.
5. **After the demo:** plan 007 on the new shape, then new slices as adoption targets.

## Demo script this buys

The existing showcase, unchanged — then "here is what kind of library this actually is": open a
project that is _not_ this repository, with the authoring slice running in it, restyled, alongside
the manifest and conformance run that got it there. The second act is the differentiator, and it
only needs one slice to be true.

## Risk to retire first

The least-proven assumption in the whole design is that the StyleX/Solid toolchain wiring in a
foreign Vite project is smooth. Spike exactly that — adopt the authoring slice into a scratch
project by hand, once — **before committing the demo date**. If it is gnarly, the fallback
(precompiled CSS for the demo build) needs lead time.

## Execution progress

Updated 2026-07-16 after implementing the first work-order item:

- [x] Plan 014 — renderer and extension clients are Effect-native, browser-safe, and covered by
      component and Workbench E2E tests.
- [x] Plan 015 — shared OpenTelemetry runtime layers, schema-owned health, bounded metrics, and the
      zero-debt architecture checker are complete. `pnpm check`, CLI E2E, and Workbench E2E pass;
      the real-Unreal map-review E2E remains the documented environment-gated skip.
- [x] Plan 004 — the persistent headless session service now owns complete cell/row intents,
      migration, review, validation, diff, and trusted transport parity.
- [x] Plan 005 — the maintained Peculiar Sheets editor now includes safe row operations, persisted
      draft navigation, dirty and validation states, and semantic Session Review with product tests.
- [x] Plan 016 demo scope — the browser-safe client contract, schema-validated HTTP transport,
      direct trusted-host composition, and headless non-Electron host are complete. The copied host
      now runs `ShedHostLive`, performs one batched native saved-project catalog operation, and opens
      selected tables through the public authoring APIs. Source conformance proves 12 fixture tables;
      the foreign `Arif_MBResearch` host proved 555 real tables and opened one snapshot. Generalized
      transports, CLI re-adaptation, remaining slice manifests, and the full Workbench refactor still
      trail after the demo as scoped above.
- [x] Fresh-agent UI-transplantation rehearsal — Cursor/Grok, Claude/Sonnet, and OpenCode/GLM
      independently produced passing clean-room build targets from the restricted kit. This proves
      source/toolchain portability only, not functional host adoption. The comparative findings are
      recorded in
      [`2026-07-17-agent-adoption-eval.md`](2026-07-17-agent-adoption-eval.md). Preserve the generated
      projects as local demo evidence; do not add live agent runs to CI or the room demo.
- [x] UI-adoption-kit remediation and Cursor regression — the kit has deterministic materialization,
      explicit production closure and provenance, target-side build verification, coherent theme
      ownership, and credential-hostile evaluation defaults. Cursor/Grok passed that narrower path.
      A later 390 px browser check showed the responsive-host claim was not actually satisfied.
- [x] Real-project host onboarding — the first OpenCode/Grok 4.5 run remains a correctly recorded
      137.2-second UI-only failed-readiness experiment. The remediated kit was then materialized into
      `Arif_MBResearch`, built, and exercised end to end: 174,026 assets scanned, 555 saved DataTables
      discovered, and `/Game/Demo/CombatCamera/DT_EnemyCameraConfig.DT_EnemyCameraConfig` opened.
      Cold catalog time was 184.06 seconds natively and 191.5 seconds through the copied HTTP host.
      Automated visual inspection was blocked by browser-tool authentication, so the running host is
      intentionally left for the owner's final visual interaction. Full results are in
      [`2026-07-17-real-project-adoption.md`](2026-07-17-real-project-adoption.md).

- [x] Follow-up project-scale remediation replaced full-file/row catalog decoding with bounded
      package-header classification, added a 37.37 MiB target-local incremental index, and exposed
      native progress through the public host transport and a visible progress bar. On the same
      174,026 assets, physical-cold native time fell to 87.22 seconds; a filesystem-warm cache miss
      through the copied host took 9.63 seconds, and a warm indexed host run took 6.56 seconds.

## Adoption risk spike result

Completed 2026-07-16 against `67a213e`. The source-StyleX path is viable; the demo does not need
the precompiled-CSS fallback.

The spike created a standalone pnpm workspace under the system temporary directory, copied only the
Data Authoring slice and its required kernel/UI packages, and rendered `AuthoringRoute` through an
in-memory `AuthoringClientShape`. It did not copy or import `apps/workbench`. The foreign host
typechecked and built 216 modules into a 209.93 kB JavaScript bundle plus 14.07 kB of extracted
StyleX CSS.

Two integration requirements surfaced and belong in demo-scoped Plan 016:

- `@stylexjs/rollup-plugin` extracts CSS during `vite build`, but its generated `stylex.css` is empty
  during `vite serve`. The foreign host must configure `runtimeInjection: command === "serve"` and
  retain file extraction for production builds. With that option, editing the copied theme changed
  the live accent from purple to orange and the browser observed the new computed color after Vite
  reloaded the theme module.
- `@ue-shed/authoring-sdk` imports review contracts from `@ue-shed/authoring`; those contracts have a
  type-only edge into the Node-backed session service. The browser host therefore needed
  `@types/node` to typecheck even though its runtime bundle was browser-safe. Plan 016 should remove
  this toolchain leakage rather than encode Node types as a permanent slice requirement.

The successful recipe is now concrete enough to define the Data Authoring manifest: copy the slice,
`ui`, and `ui-theme`; vendor the authoring/protocol kernel closure with provenance; install the
Solid, Effect, Peculiar Sheets, and StyleX dependencies; use runtime injection in development and
CSS extraction in production; then run typecheck, production build, route interaction, and token
divergence checks.
