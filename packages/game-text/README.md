# `@ue-shed/game-text`

Headless saved-package corpus discovery and search for player-facing Unreal text. The package keeps
Unreal namespace/key identity distinct from occurrence identity and reports scan coverage. Saved
packages are evidence only; this package does not mutate assets.
