# `@ue-shed/host`

Owns process lifecycle, shared Unreal sessions, static extension composition, and client-facing
domain services. It must remain UI-agnostic and observable enough for CLI diagnosis.
