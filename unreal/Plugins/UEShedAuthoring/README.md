# UEShedAuthoring

Planned optional editor plugin for generic authoring operations not available through supported stock
Unreal APIs. It depends on UEShedCore only where a shared capability is genuinely required.
